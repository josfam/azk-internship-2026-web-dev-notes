
## A tiny recap of last week

- [ ] The GitHub repository we created
- [ ] The software and extensions that we installed to help us with frontend development
- [ ] The package manager that we are using (pnpm)
- [ ] The way we had setup the project (shout out the web development repository)
- [ ] Possibly go over the frontend deployment again? 
	- [ ] Go over some important Vercel deployment options (locally first)
		- [ ] Build the project locally via `pnpm build`
		- [ ] Explore the`dist/` folder to see what the "built output" that is "shipped" to the browser looks like
			- [ ] Understand why minification is useful
		- [ ] Remind ourselves what Vite does for us "under the hood", in development and production

---
## Moving on to this week (a continuation)

### 5. Do a little clean up of the User Interface

- [ ] Move the "Add note" button to the very right of the  top header section
- [ ] Make the top section a lighter gray
- [ ] Add drop shadows on the buttons to give them that raised off the surface look
- [ ] Adjust spacing between the "You have notes" text and the "Add note" button
- [ ] Adjust the text on the browser tab, and change it from the default "frontend" to the name of the application, "encscribe"
- [ ] Give our app a favicon in the browser tab

### 5.1 Make the "Add note" button into a re-usable component

Right now, the "Add note" button is duplicated on both the `__root.tsx` and `index.tsx` files.
Duplication by itself is not necessarily a bad thing, but we can do better.

As a matter of fact, turning this into a component now will be helpful when we add the notes adding screen a little later.

- [ ] Make a new `src/components/` folder (directory)
- [ ] Inside the `src/components/` folder create a new file named `AddNoteButton.tsx`
- [ ] In the `AddNoteButton.tsx` file, create a "function component" that returns the button as UI
- [ ] Use the newly created component back in the two files that previously had the duplicated button code

### 6. An important note on mobile responsiveness

- [ ] Explore the  Mobile first extension (Browser extension) for testing the frontend on mobile devices
- [ ] Explore tailwind breakpoints for screens (how to say "Look like this on one screen, but look this other way on another screen -- in terms of size")
- [ ] Explore some UI fixes to any overflowing text. There may be some weird text behaviour a the moment

### 7. Add the form for collecting note data

- [ ] Explore Shadcn components
- [ ] Explore examples of Shadcn's `Dialog` component ()
- [ ] Call out: The form used in the example dialog is not the same as ours. we have to
